
function multiplicacao(num1: number, num2: number): number {
    return num1 + num2;
}

function saudacao(nome: string): string {
    return `Olá ${nome}`;
}
console.log(multiplicacao(20, 30)); 
console.log(saudacao("jonathan")); 